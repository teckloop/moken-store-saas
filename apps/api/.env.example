# Required
PORT=4100

# Allowed origins for CORS (comma-separated). Defaults to localhost dev ports.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176

# Optional: override SQLite database path (defaults to ./data/store.sqlite)
# DATABASE_URL=./data/store.sqlite
